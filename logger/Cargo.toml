[package]
name = "logger"
version = "0.1.0"
edition = "2021"

[features]
default = []
no-color = []


[dependencies]
log.workspace = true
shared = { path = "../shared" }
staticvec = { path = "../staticvec" }
